title: AIS JSON to NMEA
description: Convert AIS JSON messages into raw NMEA 0183 AIS sentences — pure Ruby, no native extensions.
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
